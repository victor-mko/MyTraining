import React, {Component} from 'react';

class Increment extends Component {
  render () {
    return (
      <button>+ 1</button>
    )
  }
}

export default Increment;


// onClick={store.dispach({type: 'INCREMENT'})}